/**
 * Prompt templates and output parsers for the create-agent wizard.
 * Re-exports from src/agent/create-agent-prompts for backwards compatibility.
 */
export { buildGenerationPrompt, parseGeneratedFiles, buildUpdatePrompt } from '../src/agent/create-agent-prompts';
